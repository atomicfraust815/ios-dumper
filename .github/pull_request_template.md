## Summary

Describe the change and why it is needed.

## Verification

- [ ] `cmake --build` succeeds for x64
- [ ] `ctest --output-on-failure` passes
- [ ] Signature changes were tested against the matching IOSoccer build
- [ ] No generated dumps, binaries, or process-specific diagnostics are included
